const WORKS_LIST = 'WORKS-LIST';

const worksListReducer = (state, action) => {
    switch (action.type) {
        case WORKS_LIST:
            return state;
        default:
            return state;
    }
}

export default worksListReducer;